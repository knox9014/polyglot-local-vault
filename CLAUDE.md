# CLAUDE.md — Polyglot Local Vault

이 파일은 Claude Code가 이 프로젝트를 이어받을 때 읽는 컨텍스트다.

## 프로젝트 한 줄 정의

컴퓨터의 다양한 파일을 하나의 로컬 Vault에서 초고속으로 검색·탐색·연결하고, 파일 내부 구조까지 의미 있는 객체로 관리하며, 필요할 때 외부 AI가 MCP를 통해 이 지식 구조를 사용할 수 있게 하는 **로컬 데스크톱 Workspace**.

AI가 핵심 기능을 수행하는 앱이 **아니다.** 핵심 정리·검색·분석·관계 구성은 결정론적 알고리즘으로 수행하고, AI는 MCP를 통해 선택적으로 연결한다.

## 현재 상태

```
설계 문서   v0.2 (docs/design/, 19개 문서, 3,619줄)
실측        10건 + R6 측정 (research/, 실제 저장소 18개 / 커밋 13만 / 파일 23만)
문서 검토   blocker 7, major 7, minor 3 (2026-08-17 재검증, B7 추가·해소)
R6 반영     완료 (2026-08-17, `17`/`16`/`06`/`01` — Sphinx role 추출 규칙 신설, 정본은 `17`)
18 작성     완료 (2026-08-17, 557줄 — 주소·형식·rel 정의)
형제 문서   반영 완료 (2026-08-17) — B1·B2·B3·B4·B6·M2·m2 해소
10 재작성   완료 (2026-08-17 — MVP = P0~P3 확정, `05`·`10`·`14` 수정)
계획 검토   완료 (2026-08-17 — 결점 3건 해소 + flask 재측정, `10`·`16`·`17` 수정)
m1 · m3    완료 (2026-08-17) — **미해결 항목이 전부 닫혔다**
정합성      완료 (2026-08-17 — 참조 195건 결함 0, 폐기 표현 3건 제거)
구현 코드   polyglot-vault/ 972줄, 테스트 23개 통과 (2026-08-18, 커밋 `bdd271a`)
```

**blocker 7 · major 7 · minor 3 · N1 이 전부 닫혔다.**

**Phase 0 이 완전히 끝났다** (2026-08-18) — 측정 4개(SHA 고정·기준선 재측정·게이트 모집단 정의·저장소별 하한 산출) + 구현 8개(스캔·주소·락·정합성스캔·watcher·git reader·저장소분리·Parser Adapter) 전부. `10_MVP_ROADMAP.md`의 P0 종료 조건("고정 SHA 코퍼스에서 링크 복구율 기준선이 측정되고 게이트 값·대상 범위가 확정됨")이 충족됐다. **다음은 Phase 1(Fast Search + 최소 UI)** — 데스크톱 프레임워크(Tauri/Qt) 결정이 그 전제조건.

## 게이트는 합산과 저장소별 두 가지다 (2026-08-17 확정, 2026-08-18 파일 쪽 완결)

`> 78%` 가 **무엇의 78% 인지 정의돼 있지 않았다.** 저장소별로 재보니 66.1~82.4% 로 퍼진다 — 합산 80.0% 는 심볼 수가 많은 django(16,935/20,837)에 끌려간 값이다. 저장소별 기준으로 CI 를 짰다면 4개 중 2개가 첫 실행부터 실패했을 것이다.

```
합산      자동 심볼 > 78%   (실측 80.0%,      여유 2.0%p)
저장소별  자동 심볼 > 63%   (실측 최저 66.1%, 여유 3.1%p)
합산      자동 파일 > 93%   (실측 95.7%,      여유 2.7%p)
저장소별  자동 파일 > 47%   (실측 최저 50.6%, 여유 3.6%p, requests)
적용 범위  v0.1 은 python 4개. Go/TS/Rust 파서는 Phase 2 에서야 생긴다
```

**실험3 출력표의 `지표A`/`지표B` 는 자동 복구율이 아니다** — 둘 다 1클릭 포함 수치다(`/전체`, `/생존`). 자동은 `(S1+S2)/생존` 으로 따로 계산할 것. 열 이름만 보고 인용하면 틀린다.

**파일 쪽 저장소별 하한(requests 50.6%)은 변동이 크다.** 같은 저장소를 25%/75% 경과 시점으로 재면 96.7%/97.3% — 50% 시점 하나에서만 떨어진다(생존 79건 중 39건 미해석, 표본이 작을 때 구조 개편 한 번이 통째로 걸림). 그래도 여러 시점을 평균하지 않고 심볼 게이트와 같은 방법론(50% 경과 단일 시점)을 그대로 적용했다 — 방법론을 사안마다 바꾸면 그 자체가 새 임의성이 된다. 상세는 `17_MEASUREMENT_BASIS.md` "파일 복구율의 저장소별 분포".

## 기준선 재측정 — 게이트는 유지됐다 (2026-08-17)

`17` 의 원 수치는 2026-08-16 측정인데 **그때의 HEAD 가 기록돼 있지 않았다.** `PINNED_COMMITS.txt` 의 SHA 로 체크아웃해 실험2·실험3 을 다시 돌린 결과:

```
파일 자동   95.7%  (동일)   게이트 93%, 여유 2.7%p 유지
심볼 자동   80.0%  (동일)   게이트 78%, 여유 2.0%p 유지
심볼 +1클릭 93.6%  (원 93.7% 에서 0.1%p 하락 → 전 문서 16곳 갱신)
```

**게이트는 조정하지 않았다.** 원문 `research/bench/raw_output/rebaseline_2026-08-17.txt`.

**단, N1 이 남았다** — 이 수치들은 python 4개에서 나온 것이고 `12` 는 게이트를 13개에서 돌린다고 적고 있다. 범위가 정해지기 전까지 P3 복구율 게이트는 완전히 검증된 목표가 아니다.

## 문서를 고친 뒤에는 검사기를 돌린다

```bash
python research/bench/check_refs.py docs/design      # exit 0 이어야 한다
```

상호 참조 195건(파일 참조 + 섹션 제목 인용)을 실재 여부로 대조한다. 2026-08-17 재검증에서 이 검사가 잡은 결함 2건은 **둘 다 그럴듯한 섹션 제목을 지어내서** 생긴 것이었다. 참조를 쓸 때 대상 문서의 제목을 실제로 확인할 것.

## MVP 경계 — 확정 (2026-08-17)

**MVP = P0 ~ P3.** 첫 출시는 여기서 한다. P4(MCP)·P5(Git 확장)는 첫 출시 이후다.

제품 가설("코드와 문서를 잇는 도구에 빈 자리가 있다")을 실제로 시험하는 기능은 R1 하나뿐이고 R1 은 P3 에 있다. P2 에서 멈추면 파서를 다 만들고도 가설은 한 번도 시험하지 않은 상태가 된다.

**P1 은 출시 지점이 아니라 중간 점검 지점이다.** "P1 만으로 단독 출시 가능"이라는 옛 서술을 뒤집은 것이 아니라 — 쓸 만한 것은 맞지만 검색 축은 fzf·Everything·Sourcegraph 가 이미 점유해서 P1 만 내놓으면 **가설에 대해 아무것도 배우지 못한다.** 조기 출시가 필요해지면 선택지로는 남아 있다.

**MVP 판정의 본체는 Phase 3 승인율 게이트(첫 100건 중 30건)다.** 나머지 종료 조건은 전부 "만들어졌는가"만 잰다. 미달이면 Phase 4 로 넘어가지 않는다 — 빈 그래프에 MCP 를 붙이는 셈이 된다.

## 저장소 구조

```
docs/design/          v0.2 설계 문서 18개 ← 작업의 중심
docs/design_v0.1/     원본 v0.1 (참고용. 수정하지 말 것)
research/reports/     설계 리뷰 + 벤치마크 보고서 3종 + v0.2 검토 결과
research/data/        측정 원시 데이터 (measurements.json, CSV 2종)
research/bench/       재현 가능한 측정 코드 (Rust 4 / Python 10)
TODO.md               미해결 항목 체크리스트
```

## 완료 — B7 (제안 검토 UI 규모, `16` + `09`)

`09` 목업(후보 128건)과 실측(저장소당 수천 건, cpython 6,180)의 괴리를 해소했다. 초기 노출 상한 50건 + 결정론적 랭킹 신호(임베딩·LLM 배제) + 중단 경계(판정 안 한 후보는 `.vault-ai/suggestions/`에 남아 다음 세션에 이어봄)로 정리됐다. R6/R2 자동 반영 결정(아래 "확정된 결정")으로 이 화면의 대상은 R1/R3/R4로 좁혀졌다. 상세는 `TODO.md` B7.

## 지금 해야 할 일 (순서 고정)

### 1. ~~`18` 의 결정을 형제 문서에 반영~~ — 완료 2026-08-17

`01`·`02`·`03`·`04`·`07`·`08`·`12`·`14`·`16`·`17`·`18` 수정. **B1·B2·B3·B4·B6·M2·m2 가 닫혔다.**

반영 과정에서 `18` 자체 결함 2건도 고쳤다. 뒤집기 전에 이유를 읽을 것.

```
§3.6  계단 이름이 R1~R3 이라 제안 규칙 R1~R6 과 충돌 → I1~I3
§4.4  .vault/sketches.jsonl (사라진 심볼 스냅샷, 재생성 불가) 과
      .vault-ai/ 유사도 인덱스 (현재 심볼, 재생성 가능) 는 다른 것이다.
      S4 는 앞의 것을 질의로, 뒤의 것을 후보 집합으로 쓴다
```

이 과정에서 정해진 표기 두 가지:

```
파일 해석 계단 접두사는 F1~F3   (04 가 쓰던 L1~L3 를 18 에 맞췄다)
neighbors_hint 는 {"out": {rel: n}, "in": {rel: n}}
  rel 이름에 (in) 같은 괄호를 붙이지 않는다 — JSON 키 파싱 부담
```

### 1-a. `18` 이 무엇을 정했나 (참고)

```
§1~2  vault:// 문법 — 심볼만 접두사 없음, 소제목은 #h:, slug 7단계 규칙,
      경로 정규화(NFC·대소문자 보존), 인코딩은 # % / 제어문자만
§3    타입별 해석 계단 — 파일 3단(F1a 추가) / 심볼 5단 / heading 3단 /
      JSON pointer 3단 / 열 2단 / 행·셀 2단
§4    .vault/ 파일 5종 — links(ULID·op·hint) / aliases(source=user 추가) /
      decisions(내용 해시 키) / sketches(created·refreshed) / pending
      + origin 에 extracted 신설
§5    rel 어휘 — 정방향만 저장, 역관계는 direction="in"
§6    Sphinx role → rel 매핑, 짧은 이름 참조 처리(모호하면 승인 경로로)
```

**되돌리기 비싼 결정이 여기 다 들어 있다.** 뒤집기 전에 `18` §8("미해결·측정하지 않은 것")을 먼저 읽을 것.

**S4 문제의 해법이 `18` §4.4 다** — 기억해 둘 것. 심볼 유사도 매칭(S4)은 "사라진 옛 심볼의 본문"과 현재 심볼을 비교하는데, 옛 심볼은 파일에서 이미 사라졌으므로 **재스캔으로 재생성할 수 없다.** 그래서 스케치를 `.vault/sketches.jsonl` 에 영구 저장하고, S1/S2 해석 성공 시마다 갱신한다. 스케치를 `.vault-ai/` 로 되돌리자는 제안이 나오면 이 이유로 거절할 것.

### 2. ~~CI 목표 재정의~~ — 완료 2026-08-17

**B5 해소. blocker 가 전부 닫혔다.** `05`·`06`·`10`·`12`·`14` 수정.

문제는 게이트가 도달 불가능했다는 것이다 — `93.6%` 는 사용자가 1클릭했을 때의 수치인데 CI 는 클릭하지 않는다. 자동 상한은 `80.0%` 다.

```
심볼 링크 자동 복구율   > 78%    (실측 80.0%, 여유 2.0%p)
파일 링크 자동 복구율   > 93%    (실측 95.7%, 여유 2.7%p)
1클릭 포함 수치        14 에 "제품 목표" 로 분리 — CI 게이트가 아니다
CI 러너               GitHub Actions 표준 2코어. 17 의 측정 환경과 동일 사양
고정 저장소            17 의 링크 복구 코퍼스 13개
                     SHA 는 research/bench/PINNED_COMMITS.txt, Phase 0 에서 채움
```

**적중률 게이트를 만들지 않았다.** 자동 정밀도 판정은 두 방식 다 실패했다(`17` 측정의 한계 §3). 게이트로 걸면 측정 불가능한 목표를 CI 에 거는 것이라 B5 를 새로 만드는 셈이다. 대신 후보 **생성량** 회귀만 검사한다 — 같은 입력에 같은 출력이 나오므로 재현 가능하다. 임계값은 Phase 3 기준선 측정 후 고정.

**초안 단계의 오판 3건을 기록해 둔다.** 이 문서의 옛 초안이 "적중률 > 90%" / "저장소 4개" / "4 vCPU" 를 적어뒀는데 셋 다 틀렸다. 적중률은 위 이유로 불가, 코퍼스는 `17` 에 13개가 있고, 측정 환경은 2코어다. **초안 수치를 `17` 대조 없이 반영하지 말 것.**

`TODO.md` 의 "`05` 에는 복구율 목표가 없다" 는 정정도 틀렸다 — `05:241` 에 실재했다. R6 측정의 tokio 0건 오보와 같은 계열이다. **"없다" 를 grep 패턴 하나로 판정하지 말 것.**

### 3. ~~major 7건~~ — 완료 2026-08-17

M1 · M3~M7 해소(M2 는 형제 문서 반영에서 먼저 닫혔다). `03`·`04`·`05`·`06`·`09`·`10`·`13`·`14` 수정.

**세 곳에서 "측정할 수 없는 것을 목표로 삼지 않는다" 를 적용했다.** 같은 함정이 계속 나온다.

```
M1  git 없는 vault 의 파일 복구율 88.2%
    17 확정 수치 표에 파일 단계별 breakdown 이 없다 → 정본으로 인용하지 않음
    (출처는 04 자체 측정. 참고치와 정본을 섞지 않는다)

M4  파일 저장 → 인덱스 반영 < 200ms
    17 에 없는 유일한 성능 목표 → UX 목표로 남기고 CI 게이트에서 제외

M6  P4 "3회 호출 이내로 해결"
    08 이 이미 "모델 하네스 없이는 측정 불가" 라고 적어둔 항목이었다
    → 종료 조건에서 빼고, 결정론적 구조 검증으로 교체

M7  승인율 게이트
    자동 정밀도 지표를 만들지 않았다 (17 한계 §3)
    사람이 판정하는 실사용 종료 조건이지 CI 게이트가 아니다
    30% 는 근거 없는 초기값임을 명시 — 16 의 "상한 50건" 과 같은 처리
```

**커밋 전 rename 은 복구를 보장하지 않기로 했다.** git rename detection 은 커밋 후에만 작동하고 정합성 스캔 키 `(path, mtime_ns, size)` 로는 rename 을 구분할 수 없다. 새 휴리스틱을 만드는 대신 BROKEN 으로 남는다고 정직하게 적었다. "보장 못 하는 구간을 없는 척하지 않는다" 가 이 프로젝트의 태도다.

### 4. ~~`10` 재작성~~ — 완료 2026-08-17

MVP 경계 확정(위 절). 재작성 중 **실측 근거 없는 성능 목표 1건을 새로 찾아 고쳤다** — 심볼 검색 p95 < 50ms 가 `05`·`14`·`10` 세 곳에 있었고 `17` 에 근거가 없다. M4 와 같이 UX 목표로 강등했다. `05` 각주가 M4 를 "유일한" 근거 없는 목표라고 적은 것도 정정. **"유일하다" 도 "없다" 와 같이 전수 대조 없이 판정하지 말 것.**

### 5. 계획 검토 — 완료 2026-08-17

`10` 을 `16` 과 대조해 결점 3건을 찾아 닫았다. 상세는 `TODO.md` 5-a.

**가장 중요한 것: 세대가 다른 두 측정이 섞여 있었다.** `m2_suggestions.py` 의 R1 은 `.md` 만 훑고, `m2d_dict_filter.py` 의 R1 은 `.rst`/`.txt` 도 훑는다. `.rst` 저장소에서 자릿수가 다르다(cpython 20 vs 6,180). `16` 이 R1 표엔 M2d 를, flask 합계엔 M2 의 438 을 쓰고 있었다.

flask 를 재측정해 **594(검토 큐 585)** 로 교체했다 — R1 이 0 이 아니라 156 이다. R2·R3·R4 는 샘플까지 완전 재현됐다. **수치를 인용할 때 측정 세대를 확인할 것.** 규칙 구현이 바뀌면 같은 이름의 옛 수치는 폐기된 것이다.

**R5 를 자동 반영으로 올리지 않았다.** 정적 import 라 성격은 R2(확인) 에 가깝지만 생성량조차 미측정이다. 자동 반영은 승인 없이 링크를 만드는 되돌리기 비싼 결정이므로 **확인임이 실제로 측정된 R2·R6 에만 준다.** 이 판단을 뒤집자는 제안이 나오면 R5 생성량 측정을 먼저 요구할 것.

### 6. m1 · m3 → 정합성 재검증 → Phase 0 착수

`docs/design/10_MVP_ROADMAP.md` 의 P0 절 참조. 종료 조건은 이미 `12` 의 CI 게이트(100K, 2코어, < 500ms)와 일치시켜 놓았다.

## 확정된 결정 — 뒤집지 말 것

전부 실측 근거가 있다. 조건과 출처는 `docs/design/17_MEASUREMENT_BASIS.md`.

| 결정 | 근거 |
|---|---|
| **Tree-sitter 단일 파서** | 편집 중 심볼 보존 96.9% vs CPython ast 16.7%. 함수 타이핑 중에는 99.9% vs **0.0%** |
| **저장소 2계층** (`.vault/` + `.vault-ai/`) | manual link는 재스캔으로 복구 불가한 유일한 데이터 |
| **논리 주소 + 계단식 해석** | 파일 95.7% / 심볼 93.6%. content hash·inode 기반 ID 불필요 |
| **계단 깊이는 주소 종류별로 다름** | 파일에서 이름매칭 기여 0.3%, 심볼에서 S3+S4 기여 **13.7%p** |
| **검색 기본 스코프 = 파일명** | 100K에서 경로전체 18.8ms(예산초과) vs 파일명 7.4ms |
| **파일명 검색에 역인덱스 안 씀** | `tscfgjson` → `tsconfig.json` 은 역인덱스로 표현 불가 |
| **`obj.foo()` call edge 생성 안 함** | 유일 해석률 30.3%. 반면 `foo()` 73.0%, `self.foo()` 75.3% |
| **유사도 파라미터 k=3 / sketch=32 / thr=0.40** | 45조합 스윕. 정밀도는 파라미터 무관(91.6~93.6%), 임계값만 낮추면 복구 +87% |
| **Git reader는 Phase 0** | rename 체인이 파일 7.5%p / 심볼 9.6%p 담당. 구조 개편 시 사실상 전부 |
| **Watcher + 주기 정합성 스캔, 단일 스레드** | 100K 스캔 281ms. 병렬화 시 오히려 2.3배 느림(I/O 바운드) |
| **구문(phrase) 검색 v0.1 제외** | 위치 인덱스가 5.5% → 21.8%로 확대 |
| **제안은 필터링 말고 랭킹** | 자동 정밀도 판정 2종 실패 (아래) |
| **R6 (Sphinx role 추출)** | django 675개 문서에서 측정, 밀도가 유의미한 4개 저장소가 전부 `.rst`/`.txt` (→ `17`) |
| **R6/R2는 승인 없이 자동 반영, R1은 승인 필요** | R6는 사람이 명시한 참조라 추측이 아니다. 승인 큐에 넣으면 B7 재발 |

저장 위치: R6/R2 링크는 `.vault-ai/`(자동, 문서 재파싱 시 재생성)에, 사용자가 삭제·수정한 판단은 `.vault/`(재생성 불가)에 둔다. → `16_SUGGESTION_ENGINE.md`.

## 기각된 설계 — 다시 제안하지 말 것

각각 측정으로 기각됐다. 재검토하려면 `research/reports/` 의 해당 근거를 먼저 읽을 것.

| 안 | 기각 근거 |
|---|---|
| 문자 집합 비트마스크 프리필터 | 짧고 흔한 쿼리 통과율 97%. 무효 |
| Incremental 후보 축소 (성능 목적) | 후보가 19만으로 유지돼 전체 스캔과 동일 비용 |
| 저비용 그리디 스코어링 폴백 | DP 대비 top-10 일치율 25%. `router` → `outer-forbid.stderr` 가 1위 |
| 파일 주소의 basename 매칭 단계 | 기여도 0.3%. 모호 UI 비용이 더 큼 |
| content hash / inode Stable ID | 논리 주소 계단만으로 95.7% |
| 제안의 **지역성** 필터 | django/hugo 지역성 0.0%. `docs/`와 `src/`가 분리된 표준 레이아웃에서 정답이 교차 관계 |
| 제안의 **산문 빈도** 필터 | `ModelAdmin`(산문 250회), `LoadBalancerSourceRanges`(8회) 같은 정답을 제거. 좋은 문서는 API 이름을 산문에서도 반복함 |
| CPython `ast` 파서 | 위 표 참조 |

## 작업 규칙

1. **수치의 정본은 `docs/design/17_MEASUREMENT_BASIS.md` 하나다.** 현재 같은 수치가 최대 12개 문서에 중복 기재돼 있다(검토 minor m1). 수치를 고칠 일이 생기면 17을 고치고 나머지는 참조로 바꿀 것.
2. **수치를 인용할 때는 조건을 함께 인용한다.** "7.4ms"만 쓰면 검증 불가능한 목표가 된다. "2코어 Xeon 2.1GHz, 100K 파일, 파일명 스코프, p95"까지가 한 세트다.
3. **측정하지 않은 것을 측정한 척하지 않는다.** 제안 엔진의 정밀도와 승인율은 아직 모른다. 문서에 수치를 쓰지 말 것.
4. **되돌리기 비싼 결정은 구현 전에 측정한다.** 파서 백엔드, 저장소 구조, 주소 체계가 그 범주다.
5. 문서를 고치면 상호 참조가 깨지지 않았는지 확인할 것 (현재 참조 58건).

## 재현 — 측정을 다시 돌리려면

```bash
cd research/bench
cat README.md            # 코퍼스 구성 + 실행 방법

# 의존성
pip install tree_sitter tree_sitter_python tree_sitter_go \
            tree_sitter_typescript tree_sitter_rust english-words
```

`research/data/measurements.json` 에 M1~M6 통합 결과가 있다. 공개 저장소 기반이라 누구든 재현 가능하나 HEAD가 움직이므로 수치는 정확히 재현되지 않는다. 측정일은 2026-08-16.

## 알아둘 맥락

- **제품 리스크는 그대로다.** 모든 측정이 "어떻게 만들 것인가"에 대한 것이었고 "만들 가치가 있는가"는 측정하지 않았다. 특히 제안 승인율이 낮으면 콜드스타트는 여전히 안 풀린다. P3 완료 후 실사용으로만 확인된다.
- **v0.1 메시지는 "polyglot vault"가 아니라 "문서와 코드를 잇는다"로 좁혀져 있다.** 인접 도구(fzf, Sourcegraph, Obsidian)가 각 축을 이미 점유했고, 진짜 빈 곳은 코드↔문서 링크 한 군데다. 제안 엔진의 R1 규칙이 그 가치를 증명하는 단일 기능이다.
- **P1(검색) 종료 시점에 단독으로 쓸 만한 도구가 되도록 로드맵을 잘라두었다.** 빠른 로컬 파일 검색기만으로도 가치가 있다. 다만 **첫 출시는 P1 이 아니다** — 위 "MVP 경계" 참조.
