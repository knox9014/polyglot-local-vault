# TODO — v0.2 검토 미해결 항목

근거 전문: `research/reports/04_v0.2_review_findings.md`
각 항목은 문서에서 해당 문장을 직접 확인한 것이며 추정이 아니다.

---

## BLOCKER — Phase 0 착수 전에 전부 해소

- [ ] **B1. S4 구현 불가 — 과거 심볼 스케치 저장소 없음**
  유사도 매칭은 사라진 옛 심볼의 본문이 필요한데, 스케치가 `.vault-ai/similarity/`(재생성되는 파생물)에 있다. 옛 심볼은 파일에서 사라졌으므로 재생성 불가.
  → `.vault/sketches.jsonl` 신설. 링크 생성 시점에 32개 해시 기록(레코드당 ~200B).
  영향 문서: `04`, `03`, `06`

- [ ] **B2. `vault://` 주소 문법 미정의**
  문서 전체에서 `slug` / `슬러그` / `중복 heading` / `이스케이핑` 이 0회 등장.
  → heading slug 규칙(한글 포함), fragment 네임스페이스, 이스케이핑, 경로 정규화 정의.
  영향 문서: `04` + 신규 `18`

- [ ] **B3. `links.jsonl` 레코드에 ID 없음 → tombstone·일괄 재지정 구현 불가**
  현재 스키마 `{from, rel, to, origin, ts}`. `09`의 "47건 일괄 수정"과 "마지막으로 알려진 주소"를 표현할 필드가 없다. S3/S4 "1클릭 확정" 결과를 어디 쓰는지도 미정(`04`는 alias 소스를 watcher/git 둘로 못박음).
  영향 문서: `03`, `04`, `09` + 신규 `18`

- [ ] **B4. `rel` 어휘 미정의 + 예시가 미정의 rel 사용**
  `configured_by`(01,08) `tested_by`(01,07,08) `described_by`(08) `implemented_by`(01) 이 어디에도 정의 없음. `08`은 backlinks를 `direction="in"`으로 흡수한다면서 예시에선 역관계 이름을 씀.
  `12`가 rel 표준화를 Phase 4로 미뤘으나 rel은 **Phase 3에서 이미 영구 기록**된다.
  영향 문서: `01`, `07`, `08`, `12` + 신규 `18`

- [ ] **B5. CI 회귀 테스트가 도달 불가능**
  `05`/`10`/`12`/`14` 가 "심볼 링크 복구율 > 93%"를 요구하나 93.7%는 1클릭 포함 수치. 자동 상한은 80.0%.
  파일 쪽도 목표 95% vs 실측 95.7% — 여유 0.7%p. "고정 저장소"가 미지정.
  → CLAUDE.md의 "CI 목표 재정의" 참조.

- [ ] **B6. 파일·심볼 외 주소의 해석 계단 없음**
  `04`에 "파일 3단"과 "심볼 5단"만 있음. heading / json pointer / row / cell / column 미정.
  특히 **heading 수정 시 `#slug` 가 즉시 BROKEN** — R1이 만드는 링크의 문서 쪽 끝점이고, `01`이 "이 하나만 잘 돌아가도 제품이 성립한다"고 한 기능.

---

## MAJOR

- [ ] **M1. 커밋 전 rename에 alias 소스 없음 + 문서 간 모순**
  `03` "rename은 특히 신뢰할 수 없어서" vs `04` "신뢰도는 높지만". 정합성 스캔 키가 `(path, mtime_ns, size)`라 rename을 delete+create로만 봄. git은 커밋 후에만 작동.
  → 서술 통일 + 커밋 전 구간 대응 명시 + **git 없는 vault의 복구율(88.2% / 70.4%) 표기**

- [ ] **M2. AI 제안을 `.vault-ai/`에 저장 — v0.1 모순의 재발**
  AI 제안은 재스캔으로 재생성 불가인데 "언제든 삭제 가능" 영역에 있다. Phase 0 종료 조건 "완전 복구"도 판정 불가가 됨.
  → `.vault/pending.jsonl` 로 이동. decisions ↔ 재생성 후보 매칭 키(내용 해시) 정의.

- [ ] **M3. `/` 자동 확장이 성능 예산을 스스로 깸**
  `09` "'/' 입력 시 자동 확장" + `05` 경로전체 100K = 18.8ms(예산 초과), 보장은 50K까지.
  → 결과 수 상한 / 예산 예외 / 경고 중 택일해 명시.

- [ ] **M4. `< 200 ms` 목표 vs `debounce 100~300ms` 충돌**
  debounce 상한만으로 목표 초과. 이 항목만 조건란이 비어 있고 `17` 확정 수치 표에도 없음 — **실측 근거 없는 유일한 성능 목표**.
  → `debounce 완료 후 < 200 ms` 로 재정의 + 조건란 채우기.

- [ ] **M5. R2가 `.toml`을 요구하는데 초기 형식에 없음**
  `16` R2는 "JSON / YAML / TOML" + `pyproject.toml` 예시. `06`/`14` 초기 형식엔 없고 `13`은 "향후 형식"으로 분류. `.vault/vault.toml` 자체가 TOML.
  → `.toml` 을 v0.1 지원 형식에 추가 (`06`, `10`, `13`, `14`)

- [ ] **M6. Phase 종료 조건 4건이 측정 불가/무의미**
  P0: 10K < 1s (실측 281ms@100K 대비 약 35배 느슨) / P1: 하드웨어 조건 누락 / P3: "실제 프로젝트" 미지정(TS는 12건) / P4: 평균인지 전건인지·모델 미지정
  → 전부 재작성.

- [ ] **M7. 제품 핵심 주장에 검증 게이트 없음**
  `01`은 R1에 대해 "이 하나만 잘 돌아가도 제품이 성립한다"고 하는데, Phase 3 종료 조건은 제안의 **양**만 잰다. 정밀도·승인율 게이트가 전 Phase에 없음(`10`에 산문 메모만 있고 종료 조건 블록엔 없음).
  → Phase 3 종료 조건에 승인율 게이트 추가 (예: 첫 100건 중 승인 30건 이상)

---

## MINOR

- [ ] **m1. 같은 수치가 최대 12개 문서에 중복**
  `95.7`(12개) `93.7`(10) `30.3`(10) `5.5`(8)... 수정 시 전파 누락 위험. `02` 원칙 3을 문서 패키지 자신이 위반.
  → 정본은 `17` 하나. 나머지는 참조로.

- [ ] **m2. `aliases.jsonl` / `decisions.jsonl` 스키마 없음**
  `links.jsonl` 만 예시 한 줄 존재. B3과 같은 계열.

- [ ] **m3. R1이 만드는 문서 쪽 끝점 granularity가 문서마다 다름**
  `16` 샘플은 파일 단위, `03` 예시는 heading 단위(`#teacher-router`). `16`의 R1 절에 `heading`/`섹션` 이 0회 등장. B2·B6과 맞물림.

---

## 완료 후 다음

1. `docs/design/18_DATA_FORMATS.md` 작성 완료
2. CI 목표 재정의 반영 (`05`, `10`, `12`, `14`)
3. M1~M7 즉시 수정
4. 문서 정합성 재검증 (상호 참조, 수치 일관성, 25개 반영 항목)
5. → v0.2.1 확정 후 **Phase 0 착수**
