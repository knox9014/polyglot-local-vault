# TODO — v0.2 검토 미해결 항목

근거 전문: `research/reports/04_v0.2_review_findings.md`
각 항목은 문서에서 해당 문장을 직접 확인한 것이며 추정이 아니다.

2026-08-17 재검증: B1~B6 / M1~M7 전부 문서 원문에서 재확인됨. B7 신규 추가, B1에 갱신 정책 항목 추가, B5 영향 문서 범위 정정.

2026-08-17 `18_DATA_FORMATS.md` 작성: **B1 · B1-b · B2 · B3 · B4 · B6 · M2 · m2 의 형식이 전부 정의됐다.**
다만 **정의됐다고 닫힌 것이 아니다** — 형제 문서에 옛 서술이 남아 있으면 문서끼리 모순난다.
각 항목의 "18에서 정의함 / 반영 남음"을 아래에 표시했다. 반영까지 끝나야 체크한다.

2026-08-17 **형제 문서 반영 완료.** 위 8개 항목을 체크한다. 반영 과정에서 18 자체 결함 2건도 고쳤다.

```text
18 §3.6 의 계단 이름 R1~R3 이 제안 규칙 R1~R6 과 충돌  → I1~I3 으로 변경
18 §4.4 가 "현재 살아있는 전체 심볼의 스케치 인덱스"를 정의하지 않음
  → .vault/sketches.jsonl(사라진 심볼 스냅샷, 재생성 불가)과
    .vault-ai/ 유사도 인덱스(현재 심볼, 재생성 가능)의 구분을 명시
```

부수 정리: `04` 의 파일 계단 접두사를 `L1~L3` → `F1~F3` 으로 18에 맞춤(`07`·`14`·`17` 의 참조도 함께).
`neighbors_hint` 의 역방향 표기를 `{"out":{...},"in":{...}}` 중첩으로 확정하고 18 §5.1 에 명시.
`origin` 목록에 `extracted`·`ai` 가 빠져 있던 `04`·`07` 보정.

---

## BLOCKER — Phase 0 착수 전에 전부 해소

- [x] **B1. S4 구현 불가 — 과거 심볼 스케치 저장소 없음**
  유사도 매칭은 사라진 옛 심볼의 본문이 필요한데, 스케치가 `.vault-ai/similarity/`(재생성되는 파생물)에 있다. 옛 심볼은 파일에서 사라졌으므로 재생성 불가.
  → `.vault/sketches.jsonl` 신설. 링크 생성 시점에 32개 해시 기록(레코드당 ~200B).
  영향 문서: `04`, `03`, `06`

  **B1-b. 스케치 갱신 정책 미정** (2026-08-17 추가)
  링크 생성 시점 스케치만 고정하면, 정상적으로 진화한 심볼이 오랜 시간 뒤 rename될 때
  비교 대상이 옛 스케치라 Jaccard가 0.40 아래로 떨어져 **S4가 조용히 죽는다.**
  `04`의 임계값 0.40은 "직전 커밋 vs 현재"로 스윕한 값이지 "1년 전 vs 현재"가 아니다.
  → S1/S2로 해석에 성공할 때마다 스케치를 갱신한다. 이 구간은 실측 근거가 없으므로
    파라미터를 새로 만들지 말고 보수적으로 "성공 해석 시 갱신"만 규정한다.

  **18 §4.4 에서 정의 완료** (스키마 + `created`/`refreshed` 갱신 정책).
  반영 완료 (2026-08-17): `03`(저장소 트리에 `sketches.jsonl` 추가) · `04`(스케치가 `.vault-ai/similarity/` 라는 서술 수정) · `06`

- [x] **B2. `vault://` 주소 문법 미정의**
  문서 전체에서 `slug` / `슬러그` / `중복 heading` / `이스케이핑` 이 0회 등장.
  → heading slug 규칙(한글 포함), fragment 네임스페이스, 이스케이핑, 경로 정규화 정의.
  영향 문서: `04` + 신규 `18`

  **18 §1~2 에서 정의 완료.** 심볼만 접두사 없음 / 소제목은 `#h:` / slug 7단계 규칙(한글 유지, 로케일 의존 소문자화 금지) / 중복은 `-2` + `anchor_hint` 로 흡수 / `#`·`%`·`/`·제어문자만 인코딩 / 심볼명의 `.` 은 `%2E`.
  반영 완료 (2026-08-17): `04` 의 주소 예시를 `#h:` 표기로 통일

- [x] **B3. `links.jsonl` 레코드에 ID 없음 → tombstone·일괄 재지정 구현 불가**
  현재 스키마 `{from, rel, to, origin, ts}`. `09`의 "47건 일괄 수정"과 "마지막으로 알려진 주소"를 표현할 필드가 없다. S3/S4 "1클릭 확정" 결과를 어디 쓰는지도 미정(`04`는 alias 소스를 watcher/git 둘로 못박음).
  영향 문서: `03`, `04`, `09` + 신규 `18`

  **18 §4.1 에서 정의 완료.** ULID `id` / `op: add|del|retarget` / `last_known` / `*_hint` / append-only 최종 레코드 유효.
  §4.2 에서 alias `source` 에 `user` 추가 — S3/S4 1클릭 확정 결과가 갈 곳이 생겼다.
  반영 완료 (2026-08-17): `03`(links.jsonl 예시 한 줄이 옛 스키마) · `04`(alias 소스를 watcher/git 둘로 못박은 서술)

- [x] **B4. `rel` 어휘 미정의 + 예시가 미정의 rel 사용**
  `configured_by`(01,08) `tested_by`(01,07,08) `described_by`(08) `implemented_by`(01) 이 어디에도 정의 없음. `08`은 backlinks를 `direction="in"`으로 흡수한다면서 예시에선 역관계 이름을 씀.
  `12`가 rel 표준화를 Phase 4로 미뤘으나 rel은 **Phase 3에서 이미 영구 기록**된다.
  `02:58`이 홀로 `defines` 를 쓴다 — 나머지 전 문서는 `defined_in`. 같이 정리할 것.
  영향 문서: `01`, `02`, `07`, `08`, `12` + 신규 `18`

  **18 §5 에서 정의 완료.** 정방향 어휘 목록 확정 / 역관계는 저장하지 않고 `direction="in"` 으로 조회 / 기존 역관계 표기 4종의 대응표 / `defines` → `defined_in` 통일 / 미정의 rel 은 버리지 않고 원문 보존.
  §6 에서 Sphinx role → rel 매핑도 확정.
  반영 완료 (2026-08-17): `01`·`08` 의 역관계 이름 예시 · `02:58` 의 `defines` · `12` 가 rel 표준화를 Phase 4로 미룬 서술

- [x] **B5. CI 회귀 테스트가 도달 불가능** (2026-08-17 완료)
  `10:87`/`12:116-117`/`14:171-172` 가 "심볼 링크 복구율 > 93%"를 요구하나 93.7%는 1클릭 포함 수치. 자동 상한은 80.0%.
  파일 쪽도 목표 95% vs 실측 95.7% — 여유 0.7%p. "고정 저장소"가 미지정.

  **~~2026-08-17 정정: `05` 에는 복구율 목표가 없다~~ — 이 정정 자체가 틀렸다.**
  `05:241` 에 "고정 저장소 히스토리에서 93% 이상" 이 실재했다. 영향 문서는 `05`·`06`·`10`·`12`·`14` 다섯이다.
  (교훈: "없다" 는 판정은 grep 패턴 하나로 내리지 말 것. R6 측정의 tokio 0건 오보와 같은 계열이다.)

  **해소 내용**
```text
자동 구간과 1클릭 포함을 분리    CI 는 자동만 잰다. 클릭할 사람이 없다
  심볼  > 93%  →  자동 > 78%    (실측 80.0%, 여유 2.0%p)
  파일  > 95%  →  자동 > 93%    (실측 95.7%, 여유 2.7%p)
  1클릭 포함 수치는 14 에 "제품 목표" 로 분리 보존

하드웨어 조건 명시             CI 러너 = GitHub Actions 표준 2코어
                             17 의 측정 환경과 같은 사양이라 그대로 쓴다
                             조건 없는 성능 게이트가 하나도 없도록 정리
                             (14 의 keystroke 조건이 "4코어+" 였던 오류도 정정)

고정 저장소 SHA               17 의 링크 복구 코퍼스 13개를 그대로 사용
                             research/bench/PINNED_COMMITS.txt 에 SHA 기록
                             값은 Phase 0 에서 채운다 (지어내지 않았다)

적중률 게이트는 넣지 않았다      자동 정밀도 판정이 두 방식 다 실패했다(17 한계 §3).
                             넣으면 측정 불가 목표를 CI 에 거는 것이라 B5 재발.
                             대신 후보 생성량 회귀만 검사 — 재현 가능하다
                             임계값은 Phase 3 기준선 측정 후 고정

수치 오류 2건 동시 정정         14:150 · 06:52 의 "자동 85.6~94.5%"
                             → 80.2~94.5% (85.6 은 go, 최저는 python 80.2)
```

- [x] **B6. 파일·심볼 외 주소의 해석 계단 없음**
  `04`에 "파일 3단"과 "심볼 5단"만 있음. heading / json pointer / row / cell / column 미정.
  특히 **heading 수정 시 `#slug` 가 즉시 BROKEN** — R1이 만드는 링크의 문서 쪽 끝점이고, `01`이 "이 하나만 잘 돌아가도 제품이 성립한다"고 한 기능.

  **18 §3 에서 정의 완료.** heading 3단 / JSON pointer 3단 / 열 2단 / 행·노트북 셀 2단.
  파일 계단에 `F1a`(대소문자 무시 재시도)를 추가했다 — Windows·macOS 에서 만든 링크가 Linux 에서 깨지는 것을 막는다.
  heading 은 유사도 계단을 두지 않는다(임계값 0.40 은 심볼 본문으로 스윕한 값이지 소제목 텍스트가 아니다). 대신 `anchor_hint` 텍스트 일치로 번호 밀림·오타 수정을 흡수하고, 실패 시 그 파일의 소제목 목록을 제시한다.
  반영 완료 (2026-08-17): `04` 에 타입별 계단 추가 (현재 파일 3단·심볼 5단만 있음)

- [x] **B7. 제안 검토 UI가 실측 생성량의 1/50** (2026-08-17 신규, 2026-08-17 완료, 커밋 `0854a1a`)
  `09:133` 목업은 "관계 후보 **128건**". 실측(`16:167`)은 cpython **6,180건**, django 4,721건, 8개 저장소 합 15,763건.
  `16:177`은 이 생성량을 "콜드스타트는 해결된다"의 근거로 쓰지만, 사용자 입장에서는 **6,180번 판정하라**는 뜻이다.
  초기 노출 상한도, 랭킹 기준도, 중단 경계도 문서에 없다. `09:151`의 일괄 처리는 "같은 규칙·같은 대상 파일" 단위라 이 규모를 감당하지 못한다.

  M7(승인율 게이트 없음)과 맞물린다 — **첫 100건을 무엇으로 고를지가 정의되지 않아 게이트 자체를 만들 수 없다.**

  → 초기 노출 상한(예: 상위 50) + 랭킹 신호 정의 + 나머지 후보 접근 경로.
  랭킹 신호는 **결정론적인 것만** 쓴다(임베딩·LLM 배제):
  복합 식별자 여부 / 토큰 길이 / 백틱 등장 횟수 / 심볼 docstring 유무 / 문서-심볼 이름 겹침 / 규칙 종류(R2는 경로 존재로 판정하므로 최상단).
  영향 문서: `16`, `09` (+ `18`의 `pending.jsonl` 필드에 입력이 됨)

  (2026-08-17 각주: R6/R2가 승인 없이 자동 반영되기로 결정되면서 이 화면에 들어오는 후보는 **R1/R3/R4 뿐**으로 전제가 좁아졌다. B7 자체는 해소된 상태이며 재개하지 않는다 — → `16_SUGGESTION_ENGINE.md` "승인 정책", `09_DESKTOP_UX.md` "제안 검토 UI".)

---

## MAJOR

- [x] **M1. 커밋 전 rename에 alias 소스 없음 + 문서 간 모순** (2026-08-17 완료)
  `03` "rename은 특히 신뢰할 수 없어서" vs `04` "신뢰도는 높지만". 정합성 스캔 키가 `(path, mtime_ns, size)`라 rename을 delete+create로만 봄. git은 커밋 후에만 작동.

  **해소**: 모순은 "이벤트 자체는 신뢰도가 높다, 문제는 **도착 여부**다"로 통일했다.
  커밋 전 구간은 **복구를 보장하지 않는다**고 정직하게 적었다 — 새 휴리스틱을 만들지 않고 커밋 후 git rename detection이 채울 때까지 BROKEN 으로 남는다.

  **`88.2%` 는 인용하지 않았다.** `17` 확정 수치 표에 파일 단계별 breakdown 이 없다 — 이 수치의 출처는 `04` 자체 측정이다.
  심볼 `70.4%` 만 `17` 에서 확인해 인용했다. 참고치와 정본을 섞지 않는다.

- [x] **M2. AI 제안을 `.vault-ai/`에 저장 — v0.1 모순의 재발**
  AI 제안은 재스캔으로 재생성 불가인데 "언제든 삭제 가능" 영역에 있다. Phase 0 종료 조건 "완전 복구"도 판정 불가가 됨.
  → `.vault/pending.jsonl` 로 이동. decisions ↔ 재생성 후보 매칭 키(내용 해시) 정의.

  **18 §4.5 · §4.3 에서 정의 완료.** `pending.jsonl` 스키마 + 불변 `id`, `decisions.jsonl` 의 매칭 키를 `sha256(rule + from + to)` 앞 16자리로 확정.
  반영 완료 (2026-08-17): `07`·`16`·`08` 이 AI 제안을 `.vault-ai/suggestions/` 에 둔다고 서술하는 부분

- [x] **M3. `/` 자동 확장이 성능 예산을 스스로 깸**
  `09` "'/' 입력 시 자동 확장" + `05` 경로전체 100K = 18.8ms(예산 초과), 보장은 50K까지.
  → 결과 수 상한 / 예산 예외 / 경고 중 택일해 명시.

- [x] **M4. `< 200 ms` 목표 vs `debounce 100~300ms` 충돌**
  debounce 상한만으로 목표 초과. 이 항목만 조건란이 비어 있고 `17` 확정 수치 표에도 없음 — **실측 근거 없는 유일한 성능 목표**.
  → `debounce 완료 후 < 200 ms` 로 재정의 + 조건란 채우기.

- [x] **M5. R2가 `.toml`을 요구하는데 초기 형식에 없음**
  `16` R2는 "JSON / YAML / TOML" + `pyproject.toml` 예시. `06`/`14` 초기 형식엔 없고 `13`은 "향후 형식"으로 분류. `.vault/vault.toml` 자체가 TOML.
  → `.toml` 을 v0.1 지원 형식에 추가 (`06`, `10`, `13`, `14`)

- [x] **M6. Phase 종료 조건 4건이 측정 불가/무의미** (2026-08-17 완료)
  P0: 10K < 1s (실측 281ms@100K 대비 약 35배 느슨) / P1: 하드웨어 조건 누락 / P3: "실제 프로젝트" 미지정(TS는 12건) / P4: 평균인지 전건인지·모델 미지정

  **해소**
```text
P0   정합성 스캔 < 500ms @ 100K (2코어)     12 의 CI 게이트와 같은 값
P1   세 항목 전부 (2코어) 조건 명시           17 실측 인용
P3   "실제 프로젝트" → flask (236 파일)      17 코퍼스 중 최소 규모
     후보 100건 → 300건 이상                16: R2~R5 합계 438건, 여유 138건
     R1 단독으로 잡으면 안 된다 — TypeScript 는 12건에 그치는 저장소가 있다
P4   "3회 호출 이내" 삭제                    08 이 이미 "모델 하네스 없이 측정 불가"
     → 모든 응답에 vault:// + neighbors_hint 포함 (결정론적 구조 검증)
     호출 횟수는 08 의 Phase 4 실사용 측정 항목으로 남긴다
```
  Phase 구성과 MVP 정의는 건드리지 않았다 — `10` 재작성에서 다룬다.

- [x] **M7. 제품 핵심 주장에 검증 게이트 없음** (2026-08-17 완료)
  `01`은 R1에 대해 "이 하나만 잘 돌아가도 제품이 성립한다"고 하는데, Phase 3 종료 조건은 제안의 **양**만 잰다. 정밀도·승인율 게이트가 전 Phase에 없음(`10`에 산문 메모만 있고 종료 조건 블록엔 없음).

  **해소**: Phase 3 종료 조건에 승인율 게이트 신설 — 첫 100건 중 **30건 이상 승인**.
  **자동 정밀도 지표는 만들지 않았다** — `17` 한계 §3 이 "두 방식 다 실패, 수치를 제시하지 않는 것이 정직한 상태"라고 적고 있다. B5 와 같은 함정이다.
  승인율은 사람이 판정하므로 **실사용 종료 조건이지 CI 게이트가 아니다.** 그 구분을 문서에 명시했다.
  30% 는 실측 근거 없는 초기값이며, `16` 이 "초기 노출 상한 50건"을 다룬 방식과 같게 명시하고 Phase 3 데이터로 조정한다.

---

## MINOR

- [ ] **m1. 같은 수치가 최대 12개 문서에 중복**
  `95.7`(12개) `93.7`(10) `30.3`(10) `5.5`(8)... 수정 시 전파 누락 위험. `02` 원칙 3을 문서 패키지 자신이 위반.
  → 정본은 `17` 하나. 나머지는 참조로.

- [x] **m2. `aliases.jsonl` / `decisions.jsonl` 스키마 없음**
  `links.jsonl` 만 예시 한 줄 존재. B3과 같은 계열.
  **18 §4.2 · §4.3 에서 정의 완료.** 반영 남음 없음 — 정의만 필요했던 항목이다.

- [ ] **m3. R1이 만드는 문서 쪽 끝점 granularity가 문서마다 다름**
  `16` 샘플은 파일 단위, `03` 예시는 heading 단위(`#teacher-router`). `16`의 R1 절에 `heading`/`섹션` 이 0회 등장. B2·B6과 맞물림.

---

## 완료 후 다음

1. ~~`docs/design/18_DATA_FORMATS.md` 작성~~ — **완료 2026-08-17**
2. ~~**18의 결정을 형제 문서에 반영**~~ — **완료 2026-08-17.** `01`·`02`·`03`·`04`·`07`·`08`·`12`·`14`·`16`·`17`·`18` 수정. B1·B2·B3·B4·B6·M2·m2 체크됨
3. ~~**CI 목표 재정의**~~ — **완료 2026-08-17.** `05`·`06`·`10`·`12`·`14` 수정. **B5 해소 → blocker 전부 닫힘**
4. ~~M1·M3~M7 수정~~ — **완료 2026-08-17.** `03`·`04`·`05`·`06`·`09`·`10`·`13`·`14` 수정. **major 전부 해소**
5. **`10_MVP_ROADMAP.md` 재작성 (MVP 정의)** ← 다음
   M6 의 종료 조건은 4번에서 이미 고쳤다. 남은 것은 **Phase 구성과 MVP 정의** 다.
6. **m1 · m3** — 남은 minor 2건
   - m1 수치 중복은 작업 중 계속 줄여왔다(`→17` 참조로 전환). 마지막에 한 번 훑는다
   - m3 는 B2·B6 해소로 실질 해결됐을 가능성이 있다. `16` 의 R1 절을 확인하고 판정
7. 문서 정합성 재검증 (상호 참조, 수치 일관성)
8. → v0.2.1 확정 후 **Phase 0 착수**
